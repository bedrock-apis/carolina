- Use `pnpm` for installing or updating packages
- You can use `bun` or `pnpm` to execute package scripts

## Setup

- `pnpm install`
- `bun run build`
- `cd app && bun run start`

## Read our manners and our reasoning files
